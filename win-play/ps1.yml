---
  - name: Run powershell command
    hosts: all
    become: false
    father_facts: false
    tasks:
      ansible.windows.win_powershell:
        script: |
          echo "Hello from PowerShell!"
      
